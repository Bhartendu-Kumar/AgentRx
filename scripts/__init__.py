"""Top-level reproduction drivers and scoring utilities.

Each module in this package exposes a single ``main()`` and is runnable as
``python -m scripts.<name>`` from the repo root. They were lifted here from
the gitignored ``runs/`` directory so that the public release ships a complete
reproduction story end-to-end.
"""
